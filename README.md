### This is a game I made in High-School

I just wanted to publish it because I haven't gotten around to doing so.

## Running locally

Use a normal terminal window:

```bash
python3 main.py
```

No Replit packages are required. The game reads one keypress at a time, so run it in an interactive terminal rather than inside an editor output pane.
